name: webview_inativacao_pdv
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  webview_flutter: ^0.3.22+1
  flutter_launcher_icons: ^0.7.5

flutter_icons:
  image_path: "img/icon.png"
  Android: true

dev_dependencies:
  flutter_test:
    sdk: flutter



flutter:
  uses-material-design: true
  assets:
    - img/icon.png
